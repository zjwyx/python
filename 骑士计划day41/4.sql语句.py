# 创建表
# create table 表名 (字段1 数据类型)

# 查看表结构
# desc 表名

# 插入数据
# 第一种方式
# insert into 表名 (id,name,age,sex,phon,job) values (插入的数据);
# 第二种方式
# insert into 表名 values (插入的数据);
# 第三种：插入多条数据
# insert into 表名 values (插入的数据),(插入的数据);

# 查询数据
# select * from 表名

# 查看指定列的数据
# select name,age from 表名